#pragma once
#include <iostream>
#include <string>
using std::string; using std::cout;

namespace step0{
	string READ(string s);
	string EVAL(string s);
	string PRINT(string s);
	string rep(string s);
	int run();
}